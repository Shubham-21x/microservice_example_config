server:
  port: 5005

spring:
  application:
    name: accounts-service
  datasource:
    url: jdbc:postgresql://localhost:5432/micro_test    
    username: admin                   
    password: 12345                   
  jpa:
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB